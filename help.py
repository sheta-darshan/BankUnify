import django.views.generic
help(django.views.generic)

    # apps (package)
    # conf (package)
    # contrib (package)
    # core (package)
    # db (package)
    # dispatch (package)
    # forms (package)
    # http (package)
    # middleware (package)
    # shortcuts
    # template (package)
    # templatetags (package)
    # test (package)
    # urls (package)
    # utils (package)
    # views (package)